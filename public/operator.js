const form=document.querySelector('#operator-product'),message=document.querySelector('#operator-result');
fetch('/api/admin/session',{cache:'no-store'}).then(async r=>{if(!r.ok){location.replace('/admin');return;}const data=await r.json();if(!['admin','operator'].includes(data.role)){location.replace('/admin');return;}form.hidden=false;}).catch(()=>{message.textContent='Não foi possível verificar a sessão. Atualize a página.';});
form.onsubmit=async event=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;try{const body=Object.fromEntries(new FormData(form));const r=await fetch('/api/operator/products',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw Error(r.status===409?'Serial já cadastrado. Nenhuma alteração foi feita.':r.status===403?'Sessão encerrada. Entre novamente.':'Não foi possível cadastrar. Confira os campos.');message.textContent=`Produto ${body.serial} criado com sucesso.`;form.reset();}catch(e){message.textContent=e.message;}finally{button.disabled=false;}};
document.querySelector('#operator-logout').onclick=async()=>{await fetch('/api/admin/session',{method:'DELETE'});location.replace('/admin');};
