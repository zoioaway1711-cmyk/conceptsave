# PostgreSQL com TLS, usando a URL de conexão com pooling do provedor.
DATABASE_URL=
ADMIN_USER=admin
ADMIN_PASSWORD=
# Em desenvolvimento, npm run local cria PostgreSQL local se DATABASE_URL estiver ausente.
# Vercel não permite esse fallback: configure DATABASE_URL em Production e Preview separadamente.

# WhatsApp do vendedor (DDI + DDD + número, somente dígitos). Sem valor, botão oculto.
SELLER_WHATSAPP=5511979575223
